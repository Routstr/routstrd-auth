{
  "name": "routstrd-auth",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "client": "bun run src/nip98-client.ts",
    "build": "bun build src/index.ts src/nip98-client.ts --outdir dist --target bun"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "nostr-tools": "^2.23.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21"
  }
}
